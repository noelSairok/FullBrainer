[package]
name = "fullbrainer-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = "0.38.0"
rand = "0.10.0"
# Remove sfml for now (crate feature mismatch). Re-add later if you want GUI support.